ClaSS_R: - Class State Serializer with Reflection

- uses:
	- attributes
	- reflection

- does NOT use:

- advantages:
	- it has a more simple implementation

- disadvantages:
	- slower, but not by much
	- because it uses attributes and reflection it should be more
	simple to use, but it turned out to actually be more complicated